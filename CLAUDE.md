# このリポジトリの編集ルール

発表スライドの公開場所。<https://kennyfujita.github.io/slides/private/>

**このリポジトリは公開されている。** 置いたものは誰でも読めるし、一度コミットしたら
git 履歴から消えない。以下は全部そのための決まりごと。

## 絶対にコミットしないもの

| | なぜ |
|---|---|
| **スライドのタイトル** | 公開リポジトリのファイルとして誰でも読める |
| **平文のスライド** | 同上。鍵をかけた意味が消える |
| **平文の置き場所（社内のフォルダ構成）** | パス自体が内容を説明してしまう |

これらは `decks.json` にだけ書く（`.gitignore` 済み）。**`build.py` に書き戻さないこと。**
以前 `SOURCES` として `build.py` に直書きしていた時期があり、公開リポジトリに
タイトルと社内パスが載った。その反省でこの形にしてある。

## ファイル名は内容を示唆しない文字列にする

```sh
python3 -c "import secrets; print(secrets.token_hex(6))"   # 例: 671c2510dcc7
```

発表テーマや案件名をそのまま使った名前を付けないこと。理由は3つあり、**履歴だけではない**:

1. **公開リポジトリのファイル一覧**に出る。誰でも API で取れる
2. **git 履歴に残る。** ファイル名は commit の tree に入るので、後で改名しても消えない
3. **URL としていちばん人目に触れる。** ブラウザ履歴、URL バーの補完、ブックマーク、
   画面共有、人に送ったリンク。**解錠しなくても題名が読めてしまう**

読む人は入口 `private/` の一覧から辿るので、ファイル名を人間が読む必要はない。

## コミットメッセージにタイトルを書かない

GitHub の公開イベントアーカイブ（GH Archive など）にコミットメッセージが永久保存され、
誰でも検索できる。リポジトリを消してもここには残る。
「鍵付きを1件追加」程度の書き方にとどめる。

## 構成

```
private/index.html   入口。中身が暗号文で、解錠するとタイトルの一覧が現れる
<12桁hex>.html       各スライド。中身が暗号文。タイトルは持たない
index.html           /slides/ を 404 にしないための案内だけのページ
assets/gate.css      入口・一覧・保存ボタンのスタイル
assets/crypto.js     復号そのもの。gate.js と listing.js の両方から使う
assets/gate.js       入口ページの UI。解錠して document.write する
assets/listing.js    解錠後の一覧で動く。保存ボタンを受け持つ
build.py             decks.json を読んで上を全部生成する
decks.json           タイトルと平文の場所（.gitignore 済み）
```

保存は2か所から取れる。**どちらも落ちるのは平文のスライド**で、開くのに
パスワードは要らない（暗号化したまま渡しても開くときに訊かれるだけで意味がないため）:

- 一覧の各行の「⤓ 保存」— `listing.js` が暗号文を取ってきてその場で復号する
- スライド右下の「⤓ 保存」— その場の DOM を複製する

どちらもボタン自身を取り除いてから Blob にするので、落ちたファイルを開いても
保存ボタンは出ない。ファイル名は題名そのまま（暗号文の中にしかないので問題ない）。

**タイトルを持つのは暗号文の中だけ。** 配信される HTML には
`<title>限定公開</title>` としか書かれていない。入口を解錠すると一覧が現れ、
そこからスライドへ飛ぶ。パスワードは `sessionStorage` で引き継ぐので1回で済む。

## 仕組み

| 段階 | 使うもの |
|---|---|
| 鍵導出 | PBKDF2-HMAC-SHA256、200,000 回、salt 16 バイト、512 bit |
| 暗号化 | AES-256-CBC（前半 256 bit を鍵に使う） |
| 改ざん・パスワード判定 | HMAC-SHA256（後半 256 bit）で encrypt-then-MAC |
| 本文 | 暗号化の前に gzip する |

AES は `openssl` コマンドに任せている。この Mac には `pycryptodome` も
`cryptography` も入っておらず、LibreSSL の `enc` は GCM を持たないため CBC + HMAC にした。
**数百 KB を `openssl` の stdin/stdout に流すとパイプで詰まる。** 一時ファイルを使うこと。

ブラウザ側は `assets/gate.js` が Web Crypto API で同じ手順を逆に辿る。

## 承知しておくこと

**これは「不意に見られたくない」ものを置く場所であって、秘密を守る仕組みではない。**
暗号文は誰でもダウンロードでき、手元で好きなだけパスワードを試せる（オフライン総当たり）。
PBKDF2 20 万回はその速度を落とすだけ。

- 他のサービスと同じパスワードを使わない
- 名前や誕生日を含むパスワードにしない。「名前＋4桁」は候補が約50万通りで、
  GPU 1枚なら20秒程度で開く
- 本当に外に出せないものはここに置かない。その場合は private リポジトリ +
  Netlify（`rm-calculator` と同じ形）にすれば、ファイル名も履歴も見えなくなる

## 更新のしかた

```sh
SLIDES_PASSWORD='...' python3 build.py   # decks.json を読んで全部作り直す
git add -A && git commit -m "..." && git push
```

パスワードを変えるときも `build.py` を流し直すだけ（全ページが作り直される）。

### 動作確認

```sh
python3 -m http.server 8799     # http://localhost:8799/private/
```

`crypto.subtle` は安全なコンテキストでしか使えない。https / localhost / `file:` は
どれも該当する。平文の `http://` に置くと動かない。

## 個人サイト側との関係

[`KennyFujita.github.io`](https://github.com/KennyFujita/KennyFujita.github.io) は
JavaScript なしを全体のルールにしているため、復号に JS が要るスライドは
ここに分けてある。あちらに置くのは `notes/index.html` / `ja/notes/index.html` から
`/slides/private/` への 🔒 リンク1行だけ。**リンクの文言にタイトルを書かないこと。**
